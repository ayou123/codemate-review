# LLM
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=
# Or use qwen instead:
# LLM_PROVIDER=qwen
DASHSCOPE_API_KEY=

# GitHub
GITHUB_WEBHOOK_SECRET=changeme
GITHUB_TOKEN=

# Database (overridden by docker-compose env_file)
DB_USER=codemate
DB_PASSWORD=codemate

# RAG (optional — only enable if you want project-context retrieval)
# CODEMATE_RAG_ENABLED=true
# CODEMATE_RAG_EMBEDDING_API_KEY=
